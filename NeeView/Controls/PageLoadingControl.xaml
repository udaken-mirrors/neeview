<UserControl x:Class="NeeView.PageLoadingControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:NeeView"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel x:Name="Root" VerticalAlignment="Center" HorizontalAlignment="Center">
        <StackPanel.Effect>
            <DropShadowEffect ShadowDepth="2" />
        </StackPanel.Effect>
        <TextBlock x:Name="MessageTextBlock" Text="Loading..." FontSize="20" Foreground="White" Margin="10" />
        <local:ProgressRing x:Name="ProgressRing" Foreground="White"/>
    </StackPanel>
</UserControl>
